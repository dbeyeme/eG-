# Canal LAB — hors production

Cette branche (`lab/railway-simulation`) conserve la version actuelle avec :

- API Railway + JWT + comptes seed
- Fixtures PDF de simulation (`scanner-api/fixtures/`)

**Ne jamais promouvoir cette branche vers l’alias Vercel production** (`scanner-app-gamma.vercel.app`).

La production stable pointe vers `https://voyageur241.com` (branche `main` / PRs `cursor/prod-*`).
